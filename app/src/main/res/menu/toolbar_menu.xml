<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Search", should appear as action button if possible -->
    <item
        android:id="@+id/action_search"
        android:icon="@drawable/ic_baseline_search_24"
        android:title="Search Quiz"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="ifRoom|collapseActionView" />

    <!-- Settings, should always be in the overflow -->
    <group
        android:id="@+id/action_group"
        app:showAsAction="never">
        <item
            android:id="@+id/action_change_language"
            android:title="Language"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_dark_mode"
            android:title="Dark Mode"
            android:checkable="true"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_quiz_sound"
            android:title="Quiz Sound"
            android:checkable="true"
            app:showAsAction="never" />
        <!--<item
            android:id="@+id/action_question_sound"
            android:title="Quiz Sound 1"
            android:checkable="true"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_answer_sound"
            android:title="Quiz Sound 2"
            android:checkable="true"
            app:showAsAction="never" />-->
        <item
            android:id="@+id/action_settings"
            android:title="Edit Profile"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_logout"
            android:title="Log Out"
            app:showAsAction="never" />
        <item
            android:id="@+id/action_exit"
            android:title="Exit Quiz Ocean 2.0"
            app:showAsAction="never" />
    </group>
</menu>